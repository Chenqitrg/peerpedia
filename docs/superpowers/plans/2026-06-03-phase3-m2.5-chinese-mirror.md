# M2.5: Chinese UI + ArXiv Mirror — Implementation Plan

**Goal:** Bilingual UI (CLI + Web) + arXiv article mirroring with suspended founder credit.

## Task A: Chinese UI

**Files:** `cli/main.py`, `web/templates/*.html`
**Tests:** existing tests should still pass (UI strings not tested)

## Task B: ArXiv Mirror

**Files:**
- NEW: `peerpedia/mirror.py` — arXiv API client + mirror orchestrator
- NEW: `tests/test_mirror.py` — 5 tests
- MODIFY: `peerpedia_core/storage/db.py` — add `source_arxiv_id`, `mirror_by` fields to Article
- MODIFY: `peerpedia/cli/main.py` — add `mirror` command
- NEW: `tests/test_db.py` — 1 test for new fields

**Architecture:**
```
peerpedia mirror 2301.xxxxx
  → arXiv API (export.arxiv.org) → get metadata
  → Create placeholder account for authors (arxiv:<slug>)
  → Create Article with source_arxiv_id + mirror_by
  → Award +5 points to mirror user
  → Skip review (direct to published)
```
